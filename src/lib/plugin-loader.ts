/**
 * Plugin Loader
 *
 * Simple explicit loader. Add plugins via direct import + init() call.
 */

export function loadPlugins(): void {
  // No plugins active. Add explicit imports + init() calls here as needed.
}
