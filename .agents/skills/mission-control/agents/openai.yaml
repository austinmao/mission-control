interface:
  display_name: "Mission Control"
  short_description: "Repo-specific patterns and workflows for mission-control"
  default_prompt: "Use the mission-control repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true